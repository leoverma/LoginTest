name: SwiftReviewAI

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  swiftreview:
    uses: leoverma/swiftreview-ai/.github/workflows/review.yml@main
    secrets:
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      